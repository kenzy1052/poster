import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.textpoststudio.app',
  appName: 'Text Post Studio',
  webDir: 'dist'
};

export default config;
